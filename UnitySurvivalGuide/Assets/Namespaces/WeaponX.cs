using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

/*
 * Namespaces allow us to organize our code into reusable libraries
 * - allows for flexibility with scripts of different object
 * - makes it possible to have your own defined class while another namespace has something similar with the same name.
 */

public class WeaponX : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
