using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InterfacesIntro : MonoBehaviour
{

    /// <summary>
    /// Interfaces: Similar to abstract classes in the way that they force implementation, but interfaces are basically a contract.
    ///  - Cannot contain fields
    ///  - Do not allow implementations
    ///  - Any methods within the interface, must be implemented in the class that is using it
    ///  - Methods and properties are the only things allowed
    /// </summary>

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
