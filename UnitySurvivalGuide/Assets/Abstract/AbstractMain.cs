using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// Abstraction: Forces Inheritance
/// Abstract Methods: forces Implementation
/// </summary>
public class AbstractMain : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
