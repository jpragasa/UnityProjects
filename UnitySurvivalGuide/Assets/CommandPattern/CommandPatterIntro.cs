using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommandPatterIntro : MonoBehaviour
{
    /// <summary>
    /// Command Patterns: records the events of your game or application
    /// - allows for isolation of classes, to keep track of the actions
    /// - enables the creation of a rewind system
    /// 
    /// WHEN TO USE
    /// - Demonstrates decoupling code
    /// - Easy way to record user actions
    /// 
    /// PROS
    /// - Greatly decouples code
    /// - Keeps them contained
    /// 
    /// CONS
    /// - Adds layer of complexity (more files)
    /// </summary>

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
